# react-helmet-async@2.0.5 declara peer react "^16 || ^17 || ^18" y el proyecto usa
# React 19 => npm (7+) corta con ERESOLVE. En la práctica helmet-async funciona con
# React 19. Aceptamos la resolución relajada para no bloquear el build (local y Docker).
# DEUDA: migrar a la metadata nativa de React 19 (<title>/<meta> hoisting) y quitar
# react-helmet-async; ver docs/ANALISIS_TECNICO_COMPETITIVO.md.
legacy-peer-deps=true
